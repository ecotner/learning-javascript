// the basic form
function funcName(param1, param2) { // don't need typing
    // function body
    console.log(param1 + param2);
}

// calling the function
funcName("Hello", " world");
funcName(1, 2);